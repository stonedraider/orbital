import React, { Component } from 'react';
import './Loading.css';

function Loading({ className = '' }) {
    return (
        <div
            className={className}
        >
        </div>
    );
}

export default Loading;